# Secrets — never commit
.env

# Local database
data/
*.db

# Deep-research PDF output
reports/

# Python
.venv/
__pycache__/
*.pyc
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.egg-info/

# OS / editor
.DS_Store
.vscode/*
!.vscode/settings.json

# Claude Code local tooling cache (bundled/fetched skills — regenerated on
# demand, not authored project content). If project-shared config like
# .claude/settings.json or custom commands gets added later, carve out an
# exception the same way .vscode/settings.json is above.
.claude/
.agents/